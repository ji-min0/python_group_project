def filter_text(text: str, tone: str) -> str:
    """
    입력된 text를 tone_dict.json 기준으로 치환하는 함수
    :param text: 원본 문자열
    :param tone: 치환 톤 (예: 'cute')
    :return: 치환된 문자열
    """
    return text  
